# Ensure index.php is served first for instant server-side redirect
DirectoryIndex index.php index.html

# Optional: Redirect root to coming-soon if index.html doesn't exist
# RewriteEngine On
# RewriteRule ^$ /coming-soon/en/ [R=301,L]

